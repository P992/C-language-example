#include <stdio.h>

int main ()
{
    int num;
    
    while (num != 0)
    {
          printf("Inserisci un numero (0 per terminare l'operazione):");
          scanf("%d", &num);
    }
}
